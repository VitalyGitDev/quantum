//======= CURRENT WORKAROUND=======\\
- Fix bug with no "+" button if category is empty;
- (active)Make redraw logic after successfully action
- !!!Make Service(<Data Storage> - factory service which provides specific data storage instance in Singleton mode.) for Data Storage in JSON-file!!!









//======= NEED TO DO ========\\
- MAKE CODE REVIEW ACCORDING TO PHP7.1
- Make Routes configurable!!!!!!!!!
- Make Security Control SERVICE !!!!!
- Think about how to set Permission for resources.json file edit.

- Review Model fields mapping mechanism(maybe we need to use vendors)
- Make authentication check for REST Controllers(use OAuth2.0).
- Different Services for Model Data storage(SQL-DB, NoSQL-DB, File<.json, .yml, .ini>)

- Make JS OOP for Cube!!!
- Create the way to edit hosts and nginx config files!!!

- Refactoring CSS styles and Class-names!!!
- Make Translation Service.
